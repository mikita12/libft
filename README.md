# libft

**libft** is a custom C library project from 42, recreating standard C library functions and adding useful helpers.

## Features

- Reimplementations of common C library functions (`string.h`, `ctype.h`, etc.)
- Handy utility functions
- `libft.h` header with all function prototypes
- Makefile to build the static library `libft.a`


